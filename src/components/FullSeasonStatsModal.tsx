// FullSeasonStatsModal (removed 2025-12-20) — stub retained for compatibility
import React from 'react';

export function FullSeasonStatsModal(_: { isOpen?: boolean; onClose?: () => void; currentUser?: any } = {}) {
  return null;
}

export default FullSeasonStatsModal;

